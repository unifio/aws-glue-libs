from setuptools import setup, find_packages

setup(
    name='awsglue',
    author='awslabs',
    version='0.0.1',
    python_requires='>=2.7',
    packages=find_packages(),
    keywords=['apachespark', 'spark', 'pyspark']
)
